version: 2
  build:
    docker:
      - image: mcr.microsoft/dotnet/core/sdk:2.2
    steps:
      - checkout
      - run:
        - name: Restore
        - command:
            dotnet restore
      - run:
        - name: Build
        - command:
            dotnet build
