version: 2
  jobs:
    build:
      docker:
        - image: mcr.microsoft/dotnet/core/sdk:2.2
      steps:
        - checkout
        - run:
          - name: Restore
          - command:
              dotnet restore
        - run:
          - name: Build
          - command:
              dotnet build
